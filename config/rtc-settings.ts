export default {
  /*
  |--------------------------------------------------------------------------
  | Signal Server Host
  |--------------------------------------------------------------------------
  | This is a string because dotenv does not support passing arrays in
  | Varie replaces all these values on the fly, so we cant pass it here directly.
  |
  */
  iceServers: "[]",
};
